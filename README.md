# stockacesorios

Sistema de control de inventarios con dashboard (Streamlit + SQLite + Excel Export).

## 🚀 Ejecutar localmente

1. Clona el repositorio o descarga los archivos:

```bash
git clone https://github.com/tu_usuario/stockacesorios.git
cd stockacesorios
